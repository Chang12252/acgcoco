package com.szhc.acgcoco.controllerapi;

/*import org.opencv.core.Core;
import org.opencv.core.Mat;
import org.opencv.highgui.HighGui;
import org.opencv.imgcodecs.Imgcodecs;
import org.opencv.imgproc.Imgproc;*/

import java.io.File;



public class OpencvMain {

/*    static {
        System.loadLibrary(Core.NATIVE_LIBRARY_NAME);  //加载动态链接库
    }

    public static void main(String[] args){
        String fileName = "C:\\Users\\Administrator\\Pictures\\u=2309153370,2688280452&fm=26&gp=0.jpg"; //设置图片的路径
        if (!new File(fileName).exists()){
            System.out.println("文件不存在");
        }else{
            Mat srcImg = Imgcodecs.imread(fileName);  //opencv读取
            Imgproc.cvtColor(srcImg,srcImg,6); //转化为 2 值图
         //   Imgproc.CascadeClassifier();
            srcImg.dataAddr();
        if (srcImg.empty()){
                System.out.println("加载图片失败！");
            }else{
                HighGui.imshow("image",srcImg); //显示
                HighGui.waitKey(0);
            }
        }

    }*/
    
}
